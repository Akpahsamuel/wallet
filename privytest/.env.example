# Sponsor Account Configuration (for gasless transactions)
# Only use testnet accounts - NEVER use mainnet credentials

# Option 1: Use private key (Base64 encoded)
VITE_SPONSOR_PRIVATE_KEY=""

# Option 2: Use mnemonic phrase (alternative to private key)
VITE_SPONSOR_MNEMONIC=""

# Example values (replace with your actual testnet credentials):
# VITE_SPONSOR_PRIVATE_KEY="suiprivkey1qg..."
# VITE_SPONSOR_MNEMONIC="abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"

# Network Configuration
VITE_NETWORK="testnet"

# Instructions:
# 1. Copy this file to .env
# 2. Fill in either VITE_SPONSOR_PRIVATE_KEY or VITE_SPONSOR_MNEMONIC (not both)
# 3. Make sure your sponsor account has testnet SUI tokens
# 4. Restart your development server after making changes
